{
  "name": "barcode-gen",
  "version": "1.0.0",
  "private": true,
  "description": "Self-contained barcode image generator for Cloudflare Pages, built for Excel's IMAGE() function",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-flag=nodejs_compat"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
